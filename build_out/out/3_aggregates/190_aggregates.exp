select percentile_cont(0.5) within group (order by b) from aggtest;
ERROR:  42P01: relation "aggtest" does not exist
LINE 1: ...percentile_cont(0.5) within group (order by b) from aggtest;
                                                               ^
