create or replace view agg_view1 as  select aggfns(distinct a,b,c order by a,c using ~<~,b)  from (values (1,3,'foo'),(0,null,null),(2,2,'bar'),(3,1,'baz')) v(a,b,c),  generate_series(1,2) i;
ERROR:  42883: function aggfns(integer, integer, text) does not exist
LINE 1: create or replace view agg_view1 as  select aggfns(distinct ...
                                                    ^
HINT:  No aggregate function matches the given name and argument types. Perhaps you misplaced ORDER BY; ORDER BY must appear after all regular arguments of the aggregate.
