explain (costs off)  SELECT x, y FROM  (SELECT thousand AS x, tenthous AS y FROM tenk1 a  UNION ALL  SELECT unique2 AS x, unique2 AS y FROM tenk1 b) s  ORDER BY x, y;
             QUERY PLAN             
------------------------------------
 Sort
   Sort Key: a.thousand, a.tenthous
   ->  Append
         ->  Seq Scan on tenk1 a
         ->  Seq Scan on tenk1 b
(5 rows)

