SELECT jsonb_agg(q ORDER BY x, y)  FROM rows q;
ERROR:  42P01: relation "rows" does not exist
LINE 1: SELECT jsonb_agg(q ORDER BY x, y)  FROM rows q;
                                                ^
