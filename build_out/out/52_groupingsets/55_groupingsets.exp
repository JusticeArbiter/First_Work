explain (costs off)  select a,count(*) from gstest2 group by rollup(a) having a is distinct from 1 order by a;
ERROR:  42P01: relation "gstest2" does not exist
LINE 1: explain (costs off)  select a,count(*) from gstest2 group by...
                                                    ^
