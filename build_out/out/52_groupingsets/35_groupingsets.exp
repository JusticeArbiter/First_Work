select *  from (values (1),(2)) v(x),  lateral (select a, b, sum(v.x) from gstest_data(v.x) group by rollup (a,b)) s;
ERROR:  42803: aggregate functions are not allowed in FROM clause of their own query level
LINE 1: ...rom (values (1),(2)) v(x),  lateral (select a, b, sum(v.x) f...
                                                             ^
