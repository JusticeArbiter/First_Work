select sum(c) from gstest2  group by grouping sets(a, grouping sets(a, cube(b)))  order by 1 desc;
ERROR:  42P01: relation "gstest2" does not exist
LINE 1: select sum(c) from gstest2  group by grouping sets(a, groupi...
                           ^
