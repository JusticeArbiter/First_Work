SELECT b.* FROM b LEFT JOIN a ON (b.a_id = a.id) WHERE (a.id IS NULL OR a.id > 0);
ERROR:  42703: column b.a_id does not exist
LINE 1: SELECT b.* FROM b LEFT JOIN a ON (b.a_id = a.id) WHERE (a.id...
                                          ^
