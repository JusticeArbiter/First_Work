explain (costs off)  select unique2, x.*  from int4_tbl x left join lateral (select unique1, unique2 from tenk1 where f1 = unique1) ss on true;
             QUERY PLAN              
-------------------------------------
 Hash Left Join
   Hash Cond: (x.f1 = tenk1.unique1)
   ->  Seq Scan on int4_tbl x
   ->  Hash
         ->  Seq Scan on tenk1
(5 rows)

