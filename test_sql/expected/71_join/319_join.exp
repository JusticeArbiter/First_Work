select p.*, linked from parent p  left join (select c.*, true as linked from child c) as ss  on (p.k = ss.k);
ERROR:  42P01: relation "parent" does not exist
LINE 1: select p.*, linked from parent p  left join (select c.*, tru...
                                ^
