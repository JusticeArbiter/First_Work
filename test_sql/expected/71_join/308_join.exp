explain (costs off)  select d.* from d left join (select * from b group by b.id, b.c_id) s  on d.a = s.id;
ERROR:  42703: column b.id does not exist
LINE 1: ...ct d.* from d left join (select * from b group by b.id, b.c_...
                                                             ^
HINT:  Perhaps you meant to reference the column "d.dd".
